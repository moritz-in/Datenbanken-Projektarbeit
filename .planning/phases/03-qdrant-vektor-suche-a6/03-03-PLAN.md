---
phase: 03-qdrant-vektor-suche-a6
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - repositories/qdrant_repository.py
  - services/pdf_service.py
  - routes/pdf.py
autonomous: true
requirements:
  - VECT-04
  - ROUTE-04

must_haves:
  truths:
    - "QdrantRepositoryImpl.extract_pdf_chunks() splits PDF text into 300-char chunks with page numbers"
    - "QdrantRepositoryImpl.upload_pdf_chunks() upserts chunk vectors with source_filename in payload and returns chunk count"
    - "PDFService.upload_pdf_to_qdrant() extracts, embeds, and uploads a PDF; returns a status message string"
    - "GET /pdf-upload renders pdf_upload.html without 501"
    - "POST /upload-teaching-pdf and POST /upload-product-pdf follow PRG with flash"
    - "GET /api/pdf-stats returns JSON with pdf_skripte and pdf_produkte counts"
  artifacts:
    - path: "repositories/qdrant_repository.py"
      provides: "PDF chunk methods: extract_pdf_chunks, upload_pdf_chunks, get_pdf_counts, list_uploaded_pdfs, get_unique_sources"
      contains: "extract_pdf_chunks"
    - path: "services/pdf_service.py"
      provides: "Full PDFService implementation"
      contains: "upload_pdf_to_qdrant"
    - path: "routes/pdf.py"
      provides: "GET /pdf-upload, POST /upload-teaching-pdf, POST /upload-product-pdf, GET /api/pdf-stats"
      exports: "bp"
  key_links:
    - from: "routes/pdf.py POST /upload-teaching-pdf"
      to: "ServiceFactory.get_pdf_service().upload_pdf_to_qdrant(file, COLLECTION_PDF)"
      via: "pdf_svc.upload_pdf_to_qdrant(request.files['pdf_file'], collection_name=COLLECTION_PDF)"
      pattern: "upload_pdf_to_qdrant"
    - from: "services/pdf_service.py upload_pdf_to_qdrant"
      to: "QdrantRepositoryImpl.extract_pdf_chunks + upload_pdf_chunks"
      via: "chunks = QdrantRepositoryImpl.extract_pdf_chunks(pdf_file); embeddings = embed_texts([c['text'] for c in chunks]); qdrant_repo.upload_pdf_chunks(collection_name, chunks, embeddings, filename)"
      pattern: "upload_pdf_chunks"
---

<objective>
Implement PDF chunking + embedding pipeline and wire the /pdf-upload route.

Purpose: Satisfies VECT-04 (PDF chunk indexing) and ROUTE-04 (PDF upload UI) — enables the demo's PDF-RAG tab to eventually work and shows the PDF management page.
Output: Working `/pdf-upload` page where a user can upload a PDF and see it indexed in Qdrant.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-qdrant-vektor-suche-a6/03-01-SUMMARY.md

<interfaces>
<!-- pdfplumber is already imported at the top of qdrant_repository.py -->
```python
import pdfplumber
# pdfplumber usage:
with pdfplumber.open(pdf_file) as pdf:
    for page_num, page in enumerate(pdf.pages, start=1):
        text = page.extract_text() or ""
        # split text into chunks of chunk_size characters
```

<!-- QdrantRepositoryImpl methods already implemented in Plan 01 -->
```python
qdrant_repo.ensure_collection(collection_name, vector_size=384, distance='COSINE', hnsw_m=16, hnsw_ef_construct=128)
qdrant_repo.upsert_points(collection_name, points: list[dict])  # point: {id, vector: list[float], payload: dict}
qdrant_repo.scroll(collection_name, limit=100, with_payload=True) -> (points, next_offset)
qdrant_repo.get_unique_sources(collection_name) -> set[str]  # already implemented in Plan 01
```

<!-- PDFService constructor — already wired in ServiceFactory -->
```python
class PDFService:
    def __init__(self, qdrant_repo: QdrantRepository, embedding_model: Optional[SentenceTransformer] = None):
        self.qdrant_repo = qdrant_repo
        self._embedding_model = embedding_model  # injected singleton

COLLECTION_PDF = "pdf_skripte"
COLLECTION_PDF_PRODUCTS = "pdf_produkte"
```

<!-- pdf_upload.html expects /api/pdf-stats to return JSON -->
```javascript
// JavaScript in pdf_upload.html fetches /api/pdf-stats:
// Expected JSON: { "pdf_skripte": <int>, "pdf_produkte": <int> }
```

<!-- Established route patterns from Phase 1/2 -->
```python
from services import ServiceFactory
pdf_svc = ServiceFactory.get_pdf_service()

flash("message", "success")   # green
flash("message", "danger")    # red
return redirect(url_for("pdf.pdf_upload"))  # PRG after POST
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: implement QdrantRepositoryImpl PDF methods (extract_pdf_chunks, upload_pdf_chunks, get_pdf_counts, list_uploaded_pdfs)</name>
  <files>repositories/qdrant_repository.py</files>
  <action>
    Replace `raise NotImplementedError` stubs for: `extract_pdf_chunks`, `upload_pdf_chunks`, `get_pdf_counts`, `list_uploaded_pdfs`.
    (Note: `get_unique_sources` was already implemented in Plan 01.)

    **extract_pdf_chunks(pdf_file, chunk_size=300) → list[dict]:**
    - This is a @staticmethod — no self
    - Open with `pdfplumber.open(pdf_file)` (accepts file object or BytesIO)
    - For each page, `extract_text() or ""`, then split the text into non-overlapping chunks of `chunk_size` characters
    - Each chunk: `{'text': chunk_text, 'page': page_num}` (page_num is 1-based)
    - Skip chunks where text.strip() is empty
    - Return list of chunk dicts

    **upload_pdf_chunks(collection_name, chunks, embeddings, source_filename) → int:**
    - Call `ensure_collection(collection_name, vector_size=384)` first
    - For each (chunk, embedding) pair:
      - id: use `str(uuid.uuid4())` converted to UUID — `uuid.UUID(str(uuid.uuid4()))` — Qdrant accepts UUID objects as point IDs
      - vector: `embedding.tolist() if hasattr(embedding, 'tolist') else embedding`
      - payload: `{'text': chunk['text'], 'page': chunk['page'], 'source': source_filename}`
    - Call `self._client.upsert(collection_name, points=structs, wait=True)`
    - Return `len(chunks)`

    **get_pdf_counts(pdf_collection, pdf_products_collection) → dict:**
    - For each collection: get unique sources via `get_unique_sources(collection_name)`, return `len(sources)`
    - Return `{pdf_collection: count1, pdf_products_collection: count2, 'total': count1 + count2}`
    - Handle case where collection doesn't exist: return 0 for that collection

    **list_uploaded_pdfs(collection_name) → list[str]:**
    - Call `get_unique_sources(collection_name)`
    - Return sorted list of the source strings
    - Return [] if collection doesn't exist

    Use `uuid` module (already imported at top of file).
  </action>
  <verify>
    <automated>cd /Users/stefanpavlovic/Datenbanken-Projektarbeit && python -c "
import ast
with open('repositories/qdrant_repository.py') as f:
    src = f.read()
# Check extract_pdf_chunks is no longer a stub
lines = src.split('\n')
in_method = False
for line in lines:
    if 'def extract_pdf_chunks' in line:
        in_method = True
    if in_method and 'raise NotImplementedError' in line:
        raise AssertionError('extract_pdf_chunks still has NotImplementedError stub')
    if in_method and 'def ' in line and 'extract_pdf_chunks' not in line and 'pass' not in line:
        in_method = False
# Syntax check
ast.parse(src)
print('repositories/qdrant_repository.py syntax OK, extract_pdf_chunks implemented')

# Verify extract_pdf_chunks is a staticmethod with correct output structure
from io import BytesIO
from repositories.qdrant_repository import QdrantRepositoryImpl
# Create a trivial PDF test — just verify the method exists and has correct signature
import inspect
sig = inspect.signature(QdrantRepositoryImpl.extract_pdf_chunks)
params = list(sig.parameters.keys())
assert 'pdf_file' in params, f'pdf_file param missing, got: {params}'
assert 'chunk_size' in params, f'chunk_size param missing, got: {params}'
print('extract_pdf_chunks signature OK:', params)
print('All assertions passed')
"
    </automated>
  </verify>
  <done>QdrantRepositoryImpl has zero NotImplementedError stubs in PDF methods. extract_pdf_chunks is a @staticmethod accepting pdf_file + chunk_size. upload_pdf_chunks stores source_filename in payload and returns chunk count. get_pdf_counts returns dict with collection names as keys.</done>
</task>

<task type="auto">
  <name>task 2: implement PDFService + routes/pdf.py</name>
  <files>services/pdf_service.py, routes/pdf.py</files>
  <action>
    **services/pdf_service.py** — replace all `raise NotImplementedError` stubs:

    - `_get_embedding_model()`: return `self._embedding_model` (singleton already injected — do NOT lazy-load)
    - `embed_texts(texts)`: `return self._get_embedding_model().encode(texts, show_progress_bar=False).tolist()`
    - `upload_pdf_to_qdrant(pdf_file, collection_name, chunk_size)`:
      1. `filename = getattr(pdf_file, 'filename', 'unknown.pdf')`
      2. `chunks = QdrantRepositoryImpl.extract_pdf_chunks(pdf_file, chunk_size)`
      3. If no chunks: return `"Keine Textinhalte gefunden"`
      4. `texts = [c['text'] for c in chunks]`
      5. `embeddings = self.embed_texts(texts)`
      6. `n = self.qdrant_repo.upload_pdf_chunks(collection_name, chunks, embeddings, filename)`
      7. return `f"{n} Chunks indexiert"` (this message is used by routes for flash)
    - `upload_product_pdf(pdf_file, chunk_size)`: delegate to `upload_pdf_to_qdrant(pdf_file, COLLECTION_PDF_PRODUCTS, chunk_size)`
    - `get_pdf_counts()`: delegate to `self.qdrant_repo.get_pdf_counts(COLLECTION_PDF, COLLECTION_PDF_PRODUCTS)`
    - `list_uploaded_pdfs(collection_name)`: `coll = collection_name or COLLECTION_PDF`; delegate to `self.qdrant_repo.list_uploaded_pdfs(coll)`
    - `list_teaching_pdfs()`: `self.qdrant_repo.list_uploaded_pdfs(COLLECTION_PDF)`
    - `list_product_pdfs()`: `self.qdrant_repo.list_uploaded_pdfs(COLLECTION_PDF_PRODUCTS)`
    - `ensure_collections()`: call `self.qdrant_repo.ensure_collection(COLLECTION_PDF, 384)` and `ensure_collection(COLLECTION_PDF_PRODUCTS, 384)`
    - `get_collection_stats(collection_name)`: collection = collection_name or COLLECTION_PDF; return `{'collection': collection, 'count': self.qdrant_repo.count(collection)}`
    - Add `from repositories.qdrant_repository import QdrantRepositoryImpl` import (needed for static method call)

    **routes/pdf.py** — replace all `raise NotImplementedError` stubs:

    - `pdf_upload()` GET:
      - `pdf_svc = ServiceFactory.get_pdf_service()`
      - Render `pdf_upload.html` (no template variables needed — counts are loaded via JS fetch to /api/pdf-stats)
      - Wrap in try/except; on error render with empty context

    - `upload_teaching_pdf()` POST:
      - `pdf_file = request.files.get('pdf_file')`
      - If no file or `pdf_file.filename == ''`: flash `"Keine Datei ausgewählt"`, redirect to `url_for('pdf.pdf_upload')`
      - If not `pdf_file.filename.lower().endswith('.pdf')`: flash `"Nur PDF-Dateien erlaubt"`, redirect
      - `msg = pdf_svc.upload_pdf_to_qdrant(pdf_file, collection_name='pdf_skripte')`
      - Flash `msg` with "success"; redirect to `url_for('pdf.pdf_upload')` (PRG)
      - Wrap in try/except: flash `f"Upload fehlgeschlagen: {e}"` with "danger", redirect

    - `upload_product_pdf()` POST:
      - Same pattern as upload_teaching_pdf but call `pdf_svc.upload_product_pdf(pdf_file)`

    - `get_pdf_stats()` GET:
      - `counts = pdf_svc.get_pdf_counts()`
      - Return `jsonify({'pdf_skripte': counts.get('pdf_skripte', 0), 'pdf_produkte': counts.get('pdf_produkte', 0)})`
      - Wrap in try/except: return `jsonify({'pdf_skripte': 0, 'pdf_produkte': 0})`

    Use `flash(message, "success")` / `flash(message, "danger")`.
    Use `ServiceFactory.get_pdf_service()` — never construct PDFService directly.
    Use PRG (redirect after POST) for upload handlers.
  </action>
  <verify>
    <automated>cd /Users/stefanpavlovic/Datenbanken-Projektarbeit && python -c "
import ast

for fname in ['services/pdf_service.py', 'routes/pdf.py']:
    with open(fname) as f:
        src = f.read()
    assert 'NotImplementedError' not in src, f'Stubs remain in {fname}'
    ast.parse(src)
    print(f'{fname}: syntax OK, no stubs')

# Verify flash + redirect in routes/pdf.py
with open('routes/pdf.py') as f:
    src = f.read()
assert 'flash' in src
assert 'redirect' in src
assert 'jsonify' in src
print('routes/pdf.py: flash, redirect, jsonify all present')
print('All assertions passed')
"
    </automated>
  </verify>
  <done>services/pdf_service.py and routes/pdf.py have zero NotImplementedError stubs. GET /pdf-upload renders pdf_upload.html. POST /upload-teaching-pdf and /upload-product-pdf follow PRG with flash messages. GET /api/pdf-stats returns valid JSON with pdf_skripte and pdf_produkte keys.</done>
</task>

</tasks>

<verification>
1. `python -c "from routes.pdf import bp; from services.pdf_service import PDFService"` — no ImportError
2. No `raise NotImplementedError` in `services/pdf_service.py` or `routes/pdf.py`
3. `python -c "from repositories.qdrant_repository import QdrantRepositoryImpl; import inspect; assert not inspect.isabstract(QdrantRepositoryImpl)"`
</verification>

<success_criteria>
- `repositories/qdrant_repository.py` PDF methods fully implemented: extract_pdf_chunks, upload_pdf_chunks, get_pdf_counts, list_uploaded_pdfs
- `services/pdf_service.py`: zero NotImplementedError stubs in all 9 methods
- `routes/pdf.py`: zero NotImplementedError stubs; /api/pdf-stats returns JSON; upload routes follow PRG
- extract_pdf_chunks correctly stores page numbers (1-based) and skips empty chunks
</success_criteria>

<output>
After completion, create `.planning/phases/03-qdrant-vektor-suche-a6/03-03-SUMMARY.md`
</output>
